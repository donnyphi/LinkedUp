# Needed for chat. Everything else (matching, explanation, ideas, mission) runs offline without it - those calls fall back to
# the cached and hand-written answers in backend/data/ai_cache.json.
ANTHROPIC_API_KEY=sk-ant-...
